# Build-stage only: load the supplied onnx2torch pickle and export a portable
# TorchScript classifier. These packages are absent from the final image.
numpy==2.2.6
torch==2.7.1
torchvision==0.22.1
onnx==1.18.0
onnx2torch==1.5.15
protobuf==6.31.1
