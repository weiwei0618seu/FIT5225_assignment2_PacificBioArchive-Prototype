# Frozen CPU runtime candidate. The classifier is converted from the supplied
# pickle in an isolated build stage, so ONNX/protobuf-6 never enters runtime.
boto3==1.40.0
Pillow==12.0.0
numpy==2.2.6
opencv-python-headless==4.12.0.88
torch==2.7.1
torchvision==0.22.1
protobuf==3.20.1
megadetector==10.0.24
