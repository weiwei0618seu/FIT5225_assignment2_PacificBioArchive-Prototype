Required python version is 3.12.

Pytorch is required, it will be automatically installed by megadetector.

Requirements can be install by:
python -m pip install megadetector tqdm
python -m pip install onnx2torch

Supported Classes are defined in labels.txt

After installation, run python batch.py
